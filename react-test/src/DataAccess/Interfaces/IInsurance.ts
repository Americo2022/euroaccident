/**
 * Interface to represent an insurence
 * It will help for better coding when creating objects
 */

export interface IInsurance {
	id: number;
	title: string;
	preamble: string;
	body: string;
	url: string;
}
